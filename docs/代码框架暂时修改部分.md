# 代码框架功能 - 临时修改还原指南

本文档记录了对代码框架功能所做的 **3 项临时修改**，所有原始代码均已注释保留，使用标记 `[TEMP_CHANGE_1]`、`[TEMP_CHANGE_2]`、`[TEMP_CHANGE_3]` 标识。

---

## 涉及文件

1. `static/hangfudao.js`
2. `static/framework_studio.js`

---

## 修改 1：进度显示 `[TEMP_CHANGE_1]`

**变更说明**：左上角 `进度：0/5 模块` → `共分解为 5 模块`（去掉完成数，只显示总模块数）

### 文件：`hangfudao.js`

#### 位置 A：初始 HTML 模板（约第 3520-3521 行）

**当前代码**：
```html
<!-- [TEMP_CHANGE_1] 原始: <span ...>📊 进度：<strong id="framework-progress" ...>0/1 模块</strong></span> -->
<span style="font-size: 14px; color: #64748b;">📊 共分解为 <strong id="framework-progress" style="color: #2563EB;">1 模块</strong></span>
```

**还原方法**：删除 HTML 注释行和当前 `<span>` 行，替换为：
```html
<span style="font-size: 14px; color: #64748b;">📊 进度：<strong id="framework-progress" style="color: #2563EB;">0/1 模块</strong></span>
```

#### 位置 B：动态更新函数 `recalculateCounts()`（约第 3490-3491 行）

**当前代码**：
```js
// [TEMP_CHANGE_1] 原始: progressEl.textContent = `${completed}/${total} 模块`;
progressEl.textContent = `${total} 模块`;
```

**还原方法**：删除注释行和当前行，替换为：
```js
progressEl.textContent = `${completed}/${total} 模块`;
```

---

## 修改 2：简单模块提示 `[TEMP_CHANGE_2]`

**变更说明**：当模块不需要进一步分解（`needsFurtherDecomposition=false`）时：
- 原始：`✨ 该模块已足够简单，可以直接编写代码` + `✅ 开始编写` 按钮
- 修改后：`✨ 该模块已足够简单，无需分解`（去掉按钮）

### 文件：`hangfudao.js`（约第 4098-4128 行）

原始代码在 `/* [TEMP_CHANGE_3] 原始 ... */` 块注释的 else 分支中。

**还原方法**：见下方修改 3 的还原，它们在同一个代码块中。

### 文件：`framework_studio.js`（约第 693-740 行）

原始代码在 `/* [TEMP_CHANGE_3] + [TEMP_CHANGE_2] 原始: ... */` 块注释中。

**还原方法**：见下方修改 3 的还原，它们在同一个代码块中。

---

## 修改 3：用户交互问题 `[TEMP_CHANGE_3]`

**变更说明**：当模块需要进一步分解（`needsFurtherDecomposition=true`）时：
- 原始：`你能写出 XXX 的代码吗？` + `✅ 能` 按钮 + `🔍 继续分解` 按钮
- 修改后：`你能写出 XXX 的代码吗？如果不能可以继续分解；` + 仅 `🔍 继续分解` 按钮

### 文件：`hangfudao.js` - 根卡片（约第 3652-3666 行）

**当前代码**：有 HTML 注释 `<!-- [TEMP_CHANGE_3] 原始: ... -->` 包裹的原始代码，后面是新代码。

**还原方法**：
1. 删除 `<!-- [TEMP_CHANGE_3] 原始:` 到 `-->` 之间的 HTML 注释标签（保留其中的 HTML 内容）
2. 删除 `-->` 后面的新 `<div class="fcard-gate" ...>` 块
3. 最终结果应为原始的交互决策区代码

### 文件：`hangfudao.js` - 子卡片函数（约第 4098-4128 行）

**当前代码**：`/* [TEMP_CHANGE_3] 原始 ... [TEMP_CHANGE_3] 结束 */` 块注释包含完整原始代码。

**还原方法**：
1. 删除 `/* [TEMP_CHANGE_3] 原始 needsFurtherDecomposition=true 分支:` 开始到 `[TEMP_CHANGE_3] 结束 */` 之间的块注释标记
2. 删除 `/* [TEMP_CHANGE_2] ... */` 注释行
3. 删除当前新的 `const gateHtml = ...` 代码块
4. 保留注释中的原始 `const gateHtml = ...` 代码块

### 文件：`framework_studio.js`（约第 693-740 行）

**当前代码**：`/* [TEMP_CHANGE_3] + [TEMP_CHANGE_2] 原始: ... 结束 */` 块注释包含完整原始代码。

**还原方法**：
1. 删除 `/* [TEMP_CHANGE_3] + [TEMP_CHANGE_2] 原始:` 到 `[TEMP_CHANGE_3] + [TEMP_CHANGE_2] 结束 */` 之间的块注释标记
2. 删除当前新的 `const gateHtml = ...` 代码块
3. 保留注释中的原始 `const gateHtml = ...` 代码块

---

## 快速还原方法（推荐）

在每个文件中全局搜索以下标记即可定位所有修改点：

| 标记 | 对应修改 |
|------|---------|
| `[TEMP_CHANGE_1]` | 进度显示 |
| `[TEMP_CHANGE_2]` | 简单模块提示 |
| `[TEMP_CHANGE_3]` | 用户交互问题 |

每个标记旁都有原始代码的完整注释，直接用注释中的代码替换当前代码、删除注释标记即可还原。
